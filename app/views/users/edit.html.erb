<% provide(:title, "Edit user") %> 
<% provide(:button_text, 'Save changes') %>
<h1>Update your profile</h1>
<div class="row">
    <div class="col-md-6 col-md-offset-3">
     <%= render 'form' %>
     <%# form above comes from the partial we created  %>
        <div class="gravatar_edit">
            <%= gravatar_for @user %>
            <a href="https://gravatar.com/emails" rel="nopener" target="_blank">change</a>
            <%# target = _blank gets the browser to open a new window %>
        </div>
  </div>
</div>